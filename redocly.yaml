extends:
  - recommended

# Redirect-only OIDC endpoints correctly return 302 instead of a 2XX response.
rules:
  operation-2xx-response: off
  operation-4xx-response: error
  no-unused-components: error
